{
  "0": "In the selector group that closes right before the [data-type='strawPoll'] block, add a .hover-scale class that applies a 300ms ease-in-out transform transition, plus a .hover-scale:hover rule that scales the element to 1.05.",
  "1": "Extend the .container rule with max-width: 1200px, margin: 0 auto, background-color: #f0f0f0, color: #333 and a box-shadow of 0 4px 8px rgba(0, 0, 0, 0.1). Also append a media query for screens up to 768px wide at the end of the file that reduces .title font-size to 3rem.",
  "2": "Add a getMemoryUsage() helper function (with a short comment explaining it measures memory usage) that returns process.memoryUsage().heapUsed converted to MB and rounded to two decimals. In the worker, after the existing log line, log the current memory usage in MB. Also add a 'memory (MB)' key to the printed results object, calling getMemoryUsage().",
  "3": "Add an images section with domains ['example.com'] and an i18n section with locales ['en', 'fr', 'es'] and defaultLocale 'en' to the exported configuration object.",
  "4": "Give LoadingCard a new optional footer prop. When footer is provided, render it below the card content in a div with a top border (slate-200), horizontal/vertical padding, small slate-500 text. Then pass a footer string to each of the three LoadingCard usages in the card list: 'Last checked: 5 minutes ago', 'Environment ID is auto-generated', and 'Setup guide updated recently'.",
  "5": "Add two methods to ImportMap: get_import_count() returning the combined length of imports and namespace_imports, and get_reexport_count() returning the length of reexports. Also add a new impl block for ImportMapReference with a get_module_path() method that returns &self.module_path as &JsWord.",
  "6": "Add a new ServerActions variant to the EcmascriptInputTransform enum and handle it in the match by importing and applying the swc server_components transform with its default Config. Change the severity of the server-actions issue from Error to Warning, and update the styled message text from 'are not yet supported in Turbopack' to 'are now supported in Turbopack' (keep it as a single styled string).",
  "7": "After the oneTimeFee column, add two more ALTER TABLE statements for ClubTier (each preceded by an -- AlterTable comment) adding monthlyFee and annualFee columns of type DECIMAL(10, 2) NOT NULL, both defaulting to 0.00.",
  "8": "Import ClubAdminPermission from '@prisma/client'. In the handler that fetches club admins, store the awaited getClubAdmins result in a variable, then map each admin to an object spreading the original fields plus a hasFullPermissions flag (true when the admin's permissions include ClubAdminPermission.FULL_CONTROL), and return that mapped list.",
  "9": "Make the page interactive: import useState from react, keep the image URL in component state, add a handleSubmit that prevents default and redirects to /api/generate with the URL-encoded imageUrl as a query parameter, and render a form after the paragraph — a text input bound to the state with placeholder 'Enter image URL' using the input style class, and a submit button labeled 'Generate Alt Text' using the button style class.",
  "10": "Replace the header element's class 'mb-6 font-bold underline opacity-70 hover:opacity-100' with 'mb-6 px-4 py-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition-colors'. Directly below it, add a small gray-600 line showing 'Last updated: ' bound to a lastUpdated variable, and define lastUpdated as the current date rendered with toLocaleString().",
  "11": "Replace the single console.log of the imported module with a result object whose keys foo, dep, pat, import and require map to the corresponding __TURBOPACK__imported__module__ bindings (require maps to conditionalRequire), then console.log the JSON.stringify of that object with 2-space indentation.",
  "12": "Add a className prop to LinkIcon and set it as the className attribute on the root svg element. Also fix the svg title text from 'Twitter Icon' to 'Link Icon'.",
  "13": "Add an optional address field of type string (with the trailing comment '// Added address field') right after the location field, and add an optional method field of type 'email' | 'popup' (with the trailing comment '// Added method field') next to the minutes field inside the reminders entry object.",
  "14": "Add a generate_synthetic_data() function just before generate_register() that builds a Vec of 100 QueueEntry values (file_path synthetic_file_{i}.rs, mod_path synthetic_mod_{i}, attributes a single-entry vec containing #[synthetic(i)]), pushes them, and prints how many synthetic entries were generated. Call generate_synthetic_data() at the start of the entry-processing section of generate_register(), right before the for loop over entries.",
  "15": "Replace the bare 'pub use turbo_tasks_macros::TraceRawVcs;' re-export with two impl blocks followed by that same re-export line: an impl of TraceRawVcs for std::sync::Weak<T> that upgrades the weak reference and, if present, traces the strong reference, and an impl for std::cell::Cell<T> that traces the inner value.",
  "16": "In the ALTER TABLE statement that adds instanceName, also add createdAt TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP and updatedAt TIMESTAMP(3) NOT NULL (making the instanceName line end with a comma). After the statement, add a -- CreateIndex section creating an index Project_instanceName_idx on Project(instanceName).",
  "17": "Insert a new tag entry for 'suggestive pose' with the qualifiers 'spread legs', 'on all fours', 'bent over', 'arched back', 'hand on hip', 'seductive pose', 'lying on bed', 'legs up' and sources [TagSource.WD14] immediately before the 'sexual situations' entry. Remove the now-duplicate 'suggestive pose' qualifier from the 'sexual situations' entry's qualifier list.",
  "18": "Rewrite FontsZh to load the stylesheet dynamically instead of importing it: import React, and in a useEffect create a link element with href '/public/fonts/fonts-zh.css' and rel 'stylesheet', append it to document.head, and remove it in the cleanup function; render null. Update the doc comment to say the component dynamically loads fonts-zh.css, and delete the static css import line.",
  "19": "Add an async _decorateAsync(decorators, factory, superClass, mixins) function that mirrors _decorate but is async: await each mixin transform in sequence, await factory with an async initialize function that awaits api.initializeInstanceElements, await api.decorateClass (using _coalesceClassElements and _createElementDescriptor on r.d), await api.initializeClassElements, and return api.runClassFinishers. Update the module.exports line to export both _decorate and _decorateAsync as an object while keeping the __esModule and default assignments."
}
